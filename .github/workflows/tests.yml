name: Tests

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: |
        docker-compose up | tee test_logs.md
    - name: Archive production artifacts
      uses: actions/upload-artifact@v3
      with:
        name: pytest_logs
        path: |
          test_logs.md
        retention-days: 1
